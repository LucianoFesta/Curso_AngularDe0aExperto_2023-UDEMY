export enum AuthStatus {
    checking ='checking',
    authenticated = 'authenticated',
    notAuthenticated = 'notAuthenticated',
}

//los enum se utilizan para obtener valores controlados de posibles valores.